{
  "name": "business-agent-module-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for Business Agent API",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:verbose": "vitest run --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "axios": "^1.6.2",
    "dotenv": "^17.2.3",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}

