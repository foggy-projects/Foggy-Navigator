server:
  port: 8080

spring:
  application:
    name: tutor-agent

foggy:
  tutor-agent:
    default-tenant-id: default
    agent-id: tutor-agent
    init-skills-on-startup: true

logging:
  level:
    com.foggy.navigator: DEBUG
    com.foggy.navigator.agent: DEBUG
    com.foggy.navigator.tutor: DEBUG
