---
name: ask-agent
description: 向平台上其他 Agent 提问，用于跨项目协作咨询。当用户使用 @Agent名称 提问、或提到要咨询其他项目/Agent 时使用。
---

# 咨询协作 Agent

## 前提条件

环境变量 `NAVIGATOR_TOKEN` 必须存在（由 Foggy Navigator 平台自动注入）。

如果不存在，告知用户：

> 当前未检测到 `NAVIGATOR_TOKEN` 环境变量。该变量由 Foggy Navigator 后端在派发任务时自动注入。
> 请确保你是通过 Navigator 平台派发的任务（而非直接运行 Claude Code）。

## 当前可用 Agent

| 名称 | Agent ID | 说明 |
|------|----------|------|
{{AGENT_TABLE}}

如需获取最新列表：

```bash
curl -s {{NAVIGATOR_API_BASE}}/api/v1/agents \
  -H "Authorization: Bearer $NAVIGATOR_TOKEN" | jq '.data[] | {name, description}'
```

## @ 快速选择 Agent

用户可以用 `@Agent名称` 语法直接指定要咨询的 Agent：

**匹配规则**（按优先级）：
1. **精确匹配**：`@foggy-api` → 名称完全匹配的 Agent
2. **模糊匹配**：`@api` → 名称中包含 "api" 的 Agent
3. **无匹配**：列出所有可用 Agent 让用户选择

**触发示例**：

```
用户: @foggy-api 用户认证接口的参数格式是什么？
→ 匹配 Agent "foggy-api"，向其提问"用户认证接口的参数格式是什么？"

用户: @dashboard 首页的数据刷新频率是多少？
→ 匹配名称含 "dashboard" 的 Agent

用户: 帮我问一下后端项目，登录流程是怎样的？
→ 未使用 @ 语法，根据语义从 Agent 列表中推断最匹配的 Agent
→ 如果无法确定，列出可用 Agent 并请用户用 @ 指定
```

**处理流程**：
1. 从用户消息中识别 `@xxx` 模式，提取 Agent 名称和提问内容
2. 在上方「当前可用 Agent」表中匹配 → 获取 Agent ID
3. 如果表中没有匹配，调用发现 API 动态查询最新列表
4. 匹配成功后，调用下方「向 Agent 提问」的 curl 命令执行提问
5. 将回答转述给用户，并保留 contextId 供后续追问

**多个 Agent 匹配时**：列出所有匹配项，让用户确认：
> 找到多个匹配的 Agent：
> 1. foggy-api — 后端 API 项目
> 2. foggy-api-v2 — 后端 API v2
> 请用 @完整名称 指定要咨询的 Agent。

## 向 Agent 提问

**重要**：JSON body 中如果包含非 ASCII 字符（中文等），必须先写入临时文件再用 `--data-binary @file` 发送，否则 Windows 终端编码会损坏内容导致 400 错误。

```bash
# 1) 用 python 写 JSON 到临时文件（保证 UTF-8 编码）
python3 -c "
import json
data = {'question': '你的问题内容', 'sessionId': ''}
with open('/tmp/_ask_agent.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False)
"

# 2) 用 curl 发送
curl -s -X POST {{NAVIGATOR_API_BASE}}/api/v1/agents/{agentId}/ask \
  -H "Authorization: Bearer $NAVIGATOR_TOKEN" \
  -H "Content-Type: application/json; charset=UTF-8" \
  --data-binary @/tmp/_ask_agent.json | jq '.data'
```

返回 A2aTask，取 `.data.artifacts[0].parts[0].text` 获取回答，取 `.data.contextId` 获取对话上下文 ID。

## 多轮对话

支持通过 `contextId` 进行多轮有状态对话，Agent 会记住之前的讨论内容：

```bash
# 首次提问（不传 contextId，平台自动生成）
python3 -c "
import json
data = {'question': '项目的认证机制是什么？', 'sessionId': ''}
with open('/tmp/_ask_agent.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False)
"
curl -s -X POST {{NAVIGATOR_API_BASE}}/api/v1/agents/{agentId}/ask \
  -H "Authorization: Bearer $NAVIGATOR_TOKEN" \
  -H "Content-Type: application/json; charset=UTF-8" \
  --data-binary @/tmp/_ask_agent.json | jq '.data'

# 从响应中获取 contextId，例如 "20260303-a1b2"

# 后续追问（传入 contextId，恢复对话上下文）
python3 -c "
import json
data = {'question': '那 JWT token 的过期时间是多少？', 'sessionId': '', 'contextId': '20260303-a1b2'}
with open('/tmp/_ask_agent.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False)
"
curl -s -X POST {{NAVIGATOR_API_BASE}}/api/v1/agents/{agentId}/ask \
  -H "Authorization: Bearer $NAVIGATOR_TOKEN" \
  -H "Content-Type: application/json; charset=UTF-8" \
  --data-binary @/tmp/_ask_agent.json | jq '.data'
```

## 多轮对话策略

- **何时传 contextId**：连续追问同一 Agent 同一话题时，传入上次响应的 contextId
- **何时不传**：切换到全新话题或不同 Agent 时，不传 contextId（开启新会话）
- **过期**：contextId 24 小时未使用自动过期，后续传入会开启新会话
- **自主判断**：根据对话内容判断是否需要延续上下文，无需每次都传

## 注意事项

- 回答基于目标 Agent 的项目代码分析
- 可将回答作为参考信息融入当前任务
- 如果 Agent 列表为空，可通过上方的 curl 命令动态获取
