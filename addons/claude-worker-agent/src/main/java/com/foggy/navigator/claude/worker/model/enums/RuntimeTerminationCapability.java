package com.foggy.navigator.claude.worker.model.enums;

/** Stable wire value for runtime termination support. */
public enum RuntimeTerminationCapability {
    SUPPORTED,
    UNAVAILABLE,
    UNKNOWN
}
