package com.foggy.navigator.claude.worker.model.enums;

/** Stable comparison against the task's durable Physical Worker binding. */
public enum RuntimeWorkerIdentityMatch {
    MATCHED,
    MISMATCHED,
    UNKNOWN
}
