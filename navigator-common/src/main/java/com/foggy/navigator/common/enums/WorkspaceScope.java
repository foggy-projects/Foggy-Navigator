package com.foggy.navigator.common.enums;

/**
 * Visibility boundary for an upstream-facing working directory.
 */
public enum WorkspaceScope {
    USER_PRIVATE,
    CLIENT_APP_SHARED,
    UPSTREAM_SYSTEM_SHARED
}
