package com.foggy.navigator.common.authorization;

/** Stable evaluation mode for the canonical authorization wire contract. */
public enum AuthorizationEvaluationMode {
    ENFORCEMENT,
    PREFLIGHT
}
