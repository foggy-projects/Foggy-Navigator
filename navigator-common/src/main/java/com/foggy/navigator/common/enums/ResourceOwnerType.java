package com.foggy.navigator.common.enums;

/**
 * Stable principal scope that owns an upstream-facing Navigator resource.
 */
public enum ResourceOwnerType {
    PLATFORM,
    UPSTREAM_SYSTEM,
    CLIENT_APP,
    UPSTREAM_USER
}
