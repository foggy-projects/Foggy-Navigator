package com.foggy.navigator.common.authorization.preseed;

/**
 * Offline inventory classifications. None of these values grants authority or
 * authorizes a seed operation.
 */
public enum PreseedInventoryClassification {
    VALID,
    INVALID,
    QUARANTINED
}
